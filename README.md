Hola! La verdad no tomé ninguna decisión rara además de las que fueron tomadas en la tarea 1. Modifiqué un poco los js pasados para corregir errores (validaciones) y además generalizar algunas funcionalidades, que estaban directamente hechas como para la taera pasada (Como esto de agrandar las imagenes, que lo había hecho para dos ahora es para cualquier cantidad). Modifiqué un poco el css tambien para (casi nada). Creo que no tomé ninguna decisión fuera de lo que se haya pedido, creo que lo unico fue que en detalle aviso saqué el botón para volver, porque estaba arriba en la navbar y además en una función en db.py uso el offset que basicamente mueve el lugar donde se empieza a ver la base de datos, esto lo hice para traer los datos de a 5 y no toda la base de una.